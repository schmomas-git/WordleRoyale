# WordleRoyale

A take on the NYT game Wordle with a ranked mode made with Flask.

To play, run the following commands:
`
pip install -r requirements.txt
python create_db.py
flask --app WordleRoyale run
`
